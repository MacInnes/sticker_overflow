<% if current_user %>
  <%= button_to "Add a new sticker", new_sticker_path, method: :get %>
<% end %>

<% @stickers.each do |sticker| %>
  <%= link_to sticker.title, sticker_path(sticker) %>
<% end %>
